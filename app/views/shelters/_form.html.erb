<%= simple_form_for :shelter, url: path, method: method, html: { class: "shelter"} do |f| %>
  <%= f.input :name, :label_html => { :class => 'text-white quicksand weight-bold' } %>
  <%= f.submit class: "btn btn-success" %>
<% end %>
